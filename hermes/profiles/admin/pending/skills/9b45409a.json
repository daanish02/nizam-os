{
  "id": "9b45409a",
  "subsystem": "skills",
  "action": "patch",
  "summary": "patch 'youtube-content' scripts/fetch_transcript.py (+7/-11 lines)",
  "origin": "background_review",
  "created_at": 1782606845.2956321,
  "payload": {
    "action": "patch",
    "name": "youtube-content",
    "file_path": "scripts/fetch_transcript.py",
    "old_string": "api = YouTubeTranscriptApi()\n    if languages:\n        result = api.fetch(video_id, languages=languages)\n    else:\n        result = api.fetch(video_id)\n\n    # v1.x returns FetchedTranscriptSnippet objects; normalize to dicts\n    return [\n        {\"text\": seg.text, \"start\": seg.start, \"duration\": seg.duration}\n        for seg in result\n    ]",
    "new_string": "kwargs = {\"languages\": languages} if languages else {}\n    result = YouTubeTranscriptApi.get_transcript(video_id, **kwargs)\n\n    return [\n        {\"text\": seg[\"text\"], \"start\": seg[\"start\"], \"duration\": seg[\"duration\"]}\n        for seg in result\n    ]",
    "replace_all": false
  }
}